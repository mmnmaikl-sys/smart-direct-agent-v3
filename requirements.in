# Hash-pinned production dependencies for `pip install --require-hashes`.
#
# agents-core is intentionally EXCLUDED — it is installed via pyproject.toml
# with `git+https://...@{SHA}` which pip-compile cannot hash. The deploy workflow
# loads it via `pip install -e .` AFTER --require-hashes step.

fastapi>=0.115
uvicorn>=0.30
psycopg[binary,pool]>=3.2
python-telegram-bot>=21
pydantic-settings>=2
slowapi>=0.1
Pillow>=10
